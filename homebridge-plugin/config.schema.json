{
  "pluginAlias": "HomebridgePlugin",
  "pluginType": "platform",
  "singular": true,
  "strictValidation": false,
  "schema": {
    "type": "object",
    "properties": {
      "devices": {
        "type": "array",
        "title": "Devices",
        "description": "List of devices to register with the platform.",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "type": "string",
              "title": "Name",
              "description": "Display name for the device."
            },
            "accessoryType": {
              "type": "string",
              "title": "Accessory Type",
              "description": "Type of accessory to initialize.",
              "enum": ["thermostat"]
            },
            "deviceId": {
              "type": "string",
              "title": "Device ID",
              "description": "Unique identifier for controlling the device."
            },
            "manufacturer": {
              "type": "string",
              "title": "Manufacturer",
              "description": "Manufacturer metadata for the device."
            },
            "model": {
              "type": "string",
              "title": "Model",
              "description": "Model metadata for the device."
            },
            "serialNumber": {
              "type": "string",
              "title": "Serial Number",
              "description": "Serial number metadata for the device."
            },
            "showHumidity": {
              "type": "boolean",
              "title": "Show Humidity",
              "description": "Flag whether device should show humidity."
            }
          },
          "required": ["name", "manufacturer", "model", "serialNumber", "deviceId", "accessoryType"]
        }
      }
    }
  }
}
